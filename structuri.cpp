#include <iostream>
#include <fstream>
#include <string.h>

using namespace std;

void cplxcalc()
{
        struct cplx {
                int r;
                int i;
        } z1;
        cplx z2, zr;
        char op;
        
        if()
}


int main ()
{
char C[256];
while(1){
        cout<<">";
        cin>>C;

                if (strstr(C,                    "Q!")){
                        return 0;
                } else if (strstr(C,            "TEST")){
                        TEST();
                } else if (strstr(C,            "cplx")){
                        cplxcalc();
                } else
                        printf("type \"help\" for a list of commands");
                }
        }

}
